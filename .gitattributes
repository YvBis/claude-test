# Shell scripts are executed as-is, so a CRLF checkout breaks them: on Windows
# worktrees "set -euo pipefail" arrives as "pipefail\r", which bash rejects as an
# invalid option name and the script dies before doing any work. Keep them LF in
# every checkout so the same bytes run locally and in CI.
*.sh text eol=lf

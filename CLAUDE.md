# Руководство для ИИ-агентов

Краткий указатель правил и артефактов проекта. Агент обязан следовать workflow и обращаться к артефактам при планировании и реализации.

## Workflow

### 1. Workplan

- Workplan хранится в `Roadmap.md`
- Формируется агентами на основе PRD
- Содержит только:
  - название этапа / задачи
  - короткое описание этапа / задачи
  - статус
- Не содержит деталей реализации
- Является единственным источником очереди работ

### 2. Выбор задачи

Агент выбирает задачу по правилам:

- Только задачи со статусом `todo`
- Без незавершённых зависимостей
- Самая верхняя в списке

Запрещено:

- брать несколько задач
- перескакивать
- создавать новые задачи вне Workplan

### 3. Pre-task validation

Перед началом каждой задачи агент проверяет:

- CI на main зелёный
- Docker Compose поднимается без ошибок
- Нет незакоммиченных изменений от человека

Если среда сломана — сначала починить, потом работать. Починка среды не считается задачей из Roadmap.

Если есть незакоммиченные изменения — остановиться и уточнить у пользователя.

### 4. PRD задачи

Перед началом реализации агент обязан:

- Создать PRD задачи в `/PRD`
- Выполнить декомпозицию

Ограничения:

- Подзадача <= 2 человеко-часов
- Или <= 150 строк кода

Без декомпозиции реализация запрещена.

### 5. Core Loop агента

Агент работает только через цикл:

1. Выбрать подзадачу
2. Сделать короткий ресёрч
3. Написать тесты
4. Red -> Green
5. Запустить все проверки
6. Self-review кода
7. Commit
8. Push
9. Дождаться CI

#### Шаг 6 — Self-review кода

Агент перечитывает diff своих изменений и проверяет:

- Код читаем? Имена переменных и методов понятны?
- Можно ли упростить? Нет ли лишней сложности?
- Нет мёртвого кода, неиспользуемых импортов?
- Соответствует стилю и conventions проекта?

Если находит проблемы — исправляет, перезапускает проверки (шаг 5), затем коммитит.

#### Шаг 6.1 — Внешнее ревью (3 агента)

После `ci:all` и self-review, **до** commit/push, каждое изменение ревьюят **три агента параллельно** на трёх разных моделях:

- `php-senior-reviewer` — старший Symfony/PHP-разработчик
- `architect-reviewer` — архитектор
- `tech-lead-reviewer` — техлид (скоуп, риски, готовность к merge)

Правила:

- Ревью обязательно для любой задачи/PR с изменениями кода.
- Три агента, три разные модели (конфигурация в `~/.config/opencode/agents/`).
- **Ревью считается пройденным только если все три агента вернули явный вердикт** (APPROVE / NEEDS-CHANGES / SHIP-WITH-NITS и т.п.). Агент, который упал, был отменён или не ответил, — перезапускается до получения вердикта; ревью «2 из 3» не засчитывается. Факт перезапуска фиксируется в `AssumptionLog.md`.
- Все замечания разбираются до commit/push; отклонённые — фиксируются в `AssumptionLog.md` с причиной.
- Это дополняет self-review (шаг 6), не заменяет его.

#### Шаг 9 — Ожидание CI

После push агент:

- Дожидается результата GitHub Actions
- Если CI упал — анализирует причину, исправляет, повторяет цикл с шага 5
- Не переходит к следующей задаче пока CI не зелёный

Запрещено:

- писать код без тестов
- пушить без прохождения локальных проверок
- игнорировать упавший CI
- брать новую задачу при красном CI
- пропускать self-review

### 6. Завершение задачи

После выполнения:

- Обновить статус в `Roadmap.md`
- Отметить выполненные критерии приёмки в PRD задачи (`- [ ]` → `- [x]`). Невыполненные оставлять `- [ ]` с короткой причиной. Чекбоксы обязаны быть актуальны к моменту готовности PR — не оставлять пустыми после прохождения проверок и CI
- Зафиксировать:
  - допущения
  - неясности
  - архитектурные решения

Всё записывается в `AssumptionLog.md`.

Задача считается завершённой только после записи в Assumption Log.

### 7. Периодический Review

#### Триггеры

- После завершения всех задач этапа в Roadmap
- Если прошло 5+ задач без review внутри этапа (счёт ведётся от последнего review)

#### Чеклист

| Аспект | Что проверяем |
|--------|--------------|
| Артефакты | Roadmap и PRD отражают реальность? Документация актуальна? Если пользователь вносил изменения в код — синхронизировать артефакты |
| Архитектура | Соответствует задачам и техническим требованиям? Нет дублирования? Нет признаков деградации или legacy? |
| Безопасность | Нет уязвимостей в новом коде? Валидация входных данных, авторизация, sanitization |
| CI/инфраструктура | CI работает стабильно? Нет warnings в логах? Тесты не flaky? |
| Зависимости | `composer audit` — нет known vulnerabilities? Критичные пакеты не устарели? |

#### Политика исправлений

- Найденные проблемы оформляются как новые задачи в Roadmap с пометкой `[review]`
- Берутся в работу в порядке общей очереди
- Обновление устаревшей документации делается сразу в рамках review, без отдельной задачи

#### Результат review

- Запись в `AssumptionLog.md` с итогами
- Новые задачи в `Roadmap.md` (если найдены проблемы)

### 8. Rollback-стратегия

Если задача оказалась тупиковой или ломает архитектуру:

- После 2–3 неудачных попыток решения — остановиться
- Документировать проблему в `AssumptionLog.md`
- Вернуть задачу в `todo` с пометкой `[blocked]` и причиной
- Не пытаться «дожать» бесконечно

#### Бюджет контекста задачи

- Если в ходе реализации задача превысила оценку в 2x (по строкам или по времени) — остановиться
- Передекомпозировать задачу, обновить PRD
- Разбить на более мелкие подзадачи

### 9. Smoke tests после этапа

После завершения всех задач этапа в Roadmap агент обязан запустить smoke tests:

- `docker compose up` — приложение поднимается без ошибок
- Основные URL отвечают корректно
- Если smoke test упал — исправить до перехода к следующему этапу

### 10. Changelog

После завершения этапа Roadmap агент генерирует краткий changelog:

- Что было сделано: фичи, фиксы, рефакторинг
- Формат: Markdown, файл `CHANGELOG.md` в корне проекта
- Используется для отслеживания прогресса и выявления scope creep

### 11. Артефакты проекта

Все управленческие артефакты ведутся в Markdown.

Обязательные файлы:

- `Roadmap.md` — Workplan
- `/PRD/*.md` — PRD задач
- `AssumptionLog.md` — журнал допущений
- `CHANGELOG.md` — changelog по этапам

ИИ-агенты не используют внешнюю память. Единственный источник контекста — файлы проекта.

### 12. Git-конвенции

#### 12.1 Формат коммитов

Используется Conventional Commits:

- `feat:` — новая функциональность
- `fix:` — исправление бага
- `refactor:` — рефакторинг без изменения поведения
- `test:` — добавление или изменение тестов
- `docs:` — документация
- `chore:` — инфраструктура, зависимости, CI
- `review:` — исправления по результатам периодического review

Формат: `тип: краткое описание на английском`

Для задач из Roadmap желательно указать тип и номер задачи: `feat(Collection): Task 3.3 — ...`

#### 12.2 Именование веток

`<тип>/<краткое-описание-kebab-case>`

Типы совпадают с конвенцией коммитов:

- `task/3.3-collection-service` — задача из Roadmap (тип + номер + краткое имя)
- `feat/...`, `fix/...`, `refactor/...`, `docs/...`, `test/...`, `chore/...`, `ci/...`, `review/...` — остальные случаи
- `api-doc-fixes-20260828` — legacy, не использовать (без типа)

Правила:

- kebab-case, только строчные латинские буквы и цифры
- кратко, 2–5 слов после типа
- для task-веток обязателен номер задачи из Roadmap

#### 12.3 Соавторство ИИ

Если код или изменения написаны ИИ-агентом, в теле коммита обязателен trailer:

```
Co-authored-by: <имя-инструмента> <email>
```

Пример:

```
Co-authored-by: opencode <opencode@users.noreply.github.com>
```

Правила:

- Trailer добавляется всегда, когда в коммите есть изменения, написанные или отредактированные ИИ
- Идёт в теле коммита, после описания, через пустую строку
- Имя инструмента и email должны соответствовать фактическому ИИ-инструменту, который вносил изменения (не только opencode)
- Коммит без `Co-authored-by` при ИИ-изменениях считается нарушением конвенции

#### 12.4 Merge policy

- Merge в main — **только пользователем** (или по явной команде). Агент НЕ мержит сам:
  ни через панель, ни через GitHub API (`PUT /pulls/{n}/merge`), ни локальным rebase-merge в main
- Все изменения делаются в ветках по конвенции 12.2. В main попадает только результат merge
- Рабочий цикл агента: ветка → коммит → push → PR → дождаться CI + review → **остановиться** и доложить «PR готов, мерж за тобой»
- Вопрос «Мерджить PR #N?» — подтверждение факта, НЕ команда к действию. Мержить можно только после явной команды «мержи»
- **Перед мержем PR обязан быть засквошен в один коммит** (правило сквоша). Агент сквошит по команде «засквошь»; перед финальным докладом «PR готов» ветка должна содержать один коммит. Сквош: `git reset --soft <base>` → один коммит → `push --force-with-lease` → дождаться CI на сквоше
- После merge агент вправе удалить ветку (local + remote) и синхронизировать main (pull)

## Справочные артефакты

В папке `artifacts/` хранятся исходные материалы проекта. Агент обязан обращаться к ним при формировании Roadmap, написании PRD задач и принятии архитектурных решений.

Текущий минимальный набор артефактов проекта:

| Файл | Назначение |
|------|------------|
| `artifacts/prd-taskflow-ru.md` | Продуктовое описание, цели MVP, user stories, функциональные и нефункциональные требования |
| `artifacts/technical-requirements-taskflow-ru.md` | Технические ограничения, стек, требования к архитектуре, тестам, качеству и CI/CD |
| `artifacts/domain-model-taskflow-ru.md` | Сущности, связи и базовые бизнес-правила домена |

При нехватке данных агент сначала сверяет решение с этими артефактами, а затем уже принимает самостоятельные инженерные решения в пределах зафиксированных ограничений.

Если проект опирается на внешний API или внешний контракт, агент не домысливает его поведение и проверяет детали по спецификации или reference-артефактам из локальной папки `artifacts/`.

## Тесты и проверки

- Набор команд проверок должен быть явно описан в `README.md` или рядом с задачей.
- Задача не считается завершённой без прохождения проверок и записи в `AssumptionLog.md`.

---

## Quick Reference

### Ключевые файлы

| Файл | Назначение |
|------|------------|
| `CLAUDE.md` | Workflow правила для ИИ-агентов |
| `README.md` | Обзор проекта, установка, команды |
| `Roadmap.md` | Workplan — очередь задач |
| `ARCHITECTURE.md` | Архитектурная сводка |
| `AssumptionLog.md` | Журнал допущений |
| `composer.json` | Зависимости, команды, autoload |
| `phpunit.xml.dist` | Конфигурация тестов |
| `phpstan.neon` | Конфигурация статического анализа |
| `.php-cs-fixer.dist.php` | Конфигурация стиля кода |
| `rector.php` | Конфигурация рефакторинга |
| `docker-compose.yml` | Инфраструктура |

### Структура src/

```
src/
├── Domain/           # Бизнес-логика (сущности, VO, интерфейсы репозиториев)
│   ├── User/         # Домен пользователя
│   ├── Collection/   # Домен коллекций
│   └── Common/       # Общие компоненты (UuidBinaryValue, DomainMarker)
├── Application/      # Сервисы приложения (use cases, DTO)
│   ├── User/         # Сервисы пользователя (Registration, Authentication)
│   └── Common/       # Общие компоненты (ApplicationMarker, ValidationException)
├── Infrastructure/   # Реализации (контроллеры, репозитории, провайдеры)
│   ├── Api/Controller/  # REST API контроллеры
│   ├── User/Repository/ # Doctrine репозиторий пользователя
│   ├── Collection/Repository/ # Doctrine репозиторий коллекций
│   ├── Doctrine/     # Типы, слушатели Doctrine
│   ├── Security/     # UserProvider
│   └── Common/       # InfrastructureMarker
└── Controller/       # Системные контроллеры (health check)
```

### Типичные команды

```bash
# Запуск приложения
docker compose up -d

# Миграции
docker compose exec app php bin/console doctrine:migrations:migrate

# Тесты
docker compose exec app composer test
docker compose exec app composer coverage:check

# Статический анализ
docker compose exec app composer phpstan
docker compose exec app composer phpcs:check
docker compose exec app composer rector:dry-run

# Всё вместе (CI)
docker compose exec app composer ci:all

# OpenAPI
docker compose exec app composer openapi:generate
```

### Паттерны проекта

- **Value Objects**: immutable, с бизнес-логикой в конструкторе
- **Сущности**: наследуют `AbstractEntity`, используют `ClockAwareTrait`
- **Репозитории**: интерфейс в Domain, реализация в Infrastructure
- **Маркеры слоёв**: `DomainMarker`, `ApplicationMarker`, `InfrastructureMarker`
- **UUID**: бинарный формат через `UuidBinaryValue`
- **Тесты**: PHPUnit 11.5, DAMA\DoctrineTestBundle для изоляции БД

### Текущий статус (обновлять при изменениях)

- **Этап 1 (Инфраструктура)**: ✅ завершён
- **Этап 2 (Пользователь)**: ✅ завершён
- **Этап 3 (Коллекция)**: ✅ завершён
- **Этап 4 (Айтем)**: ✅ завершён (4.1–4.7)
- **Этап 5 (Социальное)**: 🔄 core готов (5.1–5.6, 5.10); 5.11 закрыта; smoke test выполнен 20.09; этап не закрыт: периодический review — в работе; открыт review-бэклог 5.7–5.9, 5.12–5.15
- **Этап 6-8**: ⏳ в очереди

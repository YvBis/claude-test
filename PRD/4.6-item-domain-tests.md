# PRD 4.6 — Unit-тесты домена Item (весь Item-слой)

## Статус

Audit-close. Задача фактически закрыта попутно в 4.1 (ItemTest), 4.4 (Service/DTO/mapper), 4.5 (controller), 4.7 — проверено замером покрытия.

## Обоснование

- Покрытие по `src/Domain/Item/` + `src/Application/Item/` (clover, полный прогон): **311 statements, 0 uncovered** (99.59% измеряемых строк — Domain+Application).
- `src/Infrastructure/Item` (DoctrineItemRepository) намеренно исключён из coverage-gate конфигом (`phpunit.xml.dist`, `<coverage>` включает только Domain+Application); не измеряется — интеграционные тесты существуют (ghost-proxy, фильтры, поиск), покрытие не верифицировано.
- До закрытия было 10 непокрытых стейтментов (9 в `Item.php`, 1 в `ItemSlotMapper.php`).

## Изменения

| Файл | Тест | Покрывает |
|---|---|---|
| `tests/Domain/Item/Entity/ItemTest.php` | `testSetDateSlotToNullResetsSlot` | `Item::assertDateValue` null-ветку |
| `tests/Domain/Item/Entity/ItemTest.php` | `testSetBoolSlotToNullResetsSlot` | `Item::assertBoolValue` null-ветку |
| `tests/Domain/Item/Entity/ItemTest.php` | `testSetTextSlotOverMaxLengthThrows` | throw при длине > `MAX_TEXT_SLOT_LENGTH` |
| `tests/Application/Item/Service/ItemSlotMapperTest.php` | `testThrowsOnNonStringForDateSlot` | `ItemSlotMapper::asDate` non-string ветку |

## Инвентарь тестов Item-слоя (итог)

| Компонент | Тестов |
|---|---|
| `Item` entity | 50 |
| `ItemId` | 8 |
| `ItemService` | 16 |
| `ItemSlotMapper` | 21 |
| `ItemDTO` | 4 |
| `ItemNotFoundException` (косвенно через ServiceTest) | — |
| `DoctrineItemRepository` (интеграционные, MySQL) | ✓ |

## Критерии приёмки

- [x] Покрытие Item-слоя измерено и записано (311 stmts, 0 uncovered).
- [x] 4 новых теста закрывают все ранее непокрытые строки.
- [x] `ci:all` зелёный (461 тестов, 1048 ассертов, 99.59% измеряемых строк Domain+Application).
- [x] Roadmap 4.6 → done, AssumptionLog записан.

## Заметки

- `src/Infrastructure` исключён из coverage-gate осознанно (конфиг phpunit.xml.dist) — не менять без отдельного решения.
- Audit-close по прецеденту 3.5/3.6.